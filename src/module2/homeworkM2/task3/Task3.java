package module2.homeworkM2.task3;

/*public class Task3 {
    public int[] balances = {1200, 250, 2000, 500, 3200};
    public String[] ownerNames = {"Jane", "Ann", "Jack", "Oww", "Lane"};

     public  void withdraw(String name, double withdrawal) {
         int index = 0;
         for (int i = 0; i < ownerNames.length; i++) {
             if (ownerNames[i].equals(name)) {
                 index = i;
                 break;
             }

             double commision = withdrawal * 0.05;
             double balance = balances[index];
             double total = commision + withdrawal;

             if (balance < total) {
                 return name + "No";

             } else {
                 balances[index] -= total;
                 return name + " " + withdrawal + " " + balances[index];
             }

         }
     }
}*/




